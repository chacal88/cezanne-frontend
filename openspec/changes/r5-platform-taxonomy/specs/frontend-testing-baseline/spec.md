## ADDED Requirements

### Requirement: Platform taxonomy validation evidence is required
The system SHALL include automated validation for taxonomy route metadata, capability boundaries, navigation exposure, state helpers, and parent/child return behavior.

#### Scenario: Validation proves taxonomy separation
- **WHEN** taxonomy validation runs
- **THEN** it proves taxonomy routes are platform-owned and do not use settings subsection or master-data route ownership
