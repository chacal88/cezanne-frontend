# Backend/API Contract Backlog

## Purpose

This backlog audits deferred backend, API, provider, and schema blockers across V0-V5 for the next implementation phase.

Rules for this document:
- confirmed facts come from `docs/`, `src/`, and captured visual evidence only;
- unknown payloads and schemas stay unknown until backend/API contracts confirm them;
- Figma drafting status is separate from backend readiness;
- no route family is replacement-approved by this backlog.

## Evidence Baseline

Confirmed runtime/code evidence:
- Auth login uses auth service `/login`, REST `/authenticate`, and GraphQL session enrichment in `src/domains/auth/api/auth-api-adapter.ts`.
- Public auth token helpers call `/user/forgot-password`, `/user/valid-token`, `/user/reset-password`, `/user/first-access`, `/invitation`, `/hiring_company`, and `/recruitment_agency`.
- Cezanne/SAML callbacks exchange provider codes through auth service callback routes before the same bootstrap path.
- Dashboard has an API adapter in `src/domains/dashboard/api/dashboard-api.ts` for GraphQL dashboard aggregates, REST `/notification/notifications`, and GraphQL calendar reads.
- Candidate database has a REST adapter for `/v2/cv` in `src/domains/candidates/database/candidate-database-api.ts`.
- Candidate detail has a GraphQL adapter in `src/domains/candidates/detail-hub/candidate-detail-api.ts`.
- Notifications, inbox, account settings, Jobs, many V3 public/token flows, V4 operations, and V5 SysAdmin/platform routes still use fixture/state/adaptor seams where production contracts are not confirmed.

## Blocking Levels

| Level | Meaning |
|---|---|
| `P0 backend contract blocker` | Production implementation cannot replace fixture/state behavior without a backend/API decision. |
| `P1 schema/payload blocker` | Route-state shell can continue, but field-specific UI, validation, mutation, or parity work must wait. |
| `P2 provider/transport blocker` | Screen-flow can proceed with annotated placeholders, but live integration/provider behavior is blocked. |
| `P3 evidence/parity blocker` | Backend contract may be enough for data work, but replacement approval still needs visual/parity evidence. |

## V0-V5 Matrix

| Release | Route family | Known contract | Unknown contract | Owner/question | Blocking level |
|---|---|---|---|---|---|
| V0 | Auth entry `/`, `/login` | `/login` accepts email/password/code and returns `token`; `/authenticate` returns `user`; GraphQL bootstrap returns shell/session enrichment. Current mapper recognizes activation, SSO mandatory, approval, 2FA required/failed, setup required, invalid credentials, and bootstrap failed from message text. | Final auth error taxonomy/copy, 2FA delivery method, retry/lockout policy, activation/setup reason payloads, and whether errors become structured codes instead of message substrings. | Backend/Auth: publish structured login failure envelope and 2FA policy. Should `/login` continue accepting `code`, or should 2FA use a challenge endpoint? | P0 backend contract blocker |
| V0 | Token flows | Forgot/reset/confirm/register/invite REST paths and minimal `msg` values are consumed. Visual states cover missing/invalid/expired/valid/success/failure/retry/pending approval/bootstrap failure. | Invite continuation payload, registration approval semantics, token validation schema, token expiration/used/pending reasons, and final continuation UI fields. | Backend/Auth: define token lifecycle enum and continuation payload per token family. Which token families return a session token versus terminal pending approval? | P0 backend contract blocker |
| V0 | Cezanne/SAML launch and callbacks | Cezanne launch redirects to `/login/cezanne/:tenantGuid`; callback reads `code` through `/login/cezanne/callback?json=1&code=...`; SAML callback reads `code`; SAML launch accepts profile email. | Provider callback payloads, provider error taxonomy/copy, tenant lookup response, popup/redirect contract, missing-tenant behavior, and callback exchange failure envelope. | Backend/Auth/Provider: publish provider launch/callback response envelope and error enum for Cezanne and SAML. | P0 backend contract blocker |
| V0 | Logout and session loss | Legacy `/logout` calls `LoginService.logout()` and redirects `/`. Legacy refresh failure handles `old_token` with a warning and home redirect; other refresh failures call logout. | Dedicated `/session-lost` route/copy was not located in legacy/auth-service sources; whether session loss is old-token warning, silent logout, or a new current-app-only screen is undecided. | Product/Auth: decide `/session-lost` copy, trigger taxonomy, and whether it can differ from explicit `/logout`. | P0 backend contract blocker |
| V0 | Account profiles | Runtime exposes fixture states for ready/dirty/saving/saved/save-failed/retry/degraded/denied and local validation. | Persistence API, server validation schema, user/company/agency field ownership, stale/conflict behavior, and account-menu parity. | Backend/Account: provide read/update schemas and validation error envelope for profile settings. | P1 schema/payload blocker |
| V0 | Notifications | Typed destination resolver and fallback behavior are implemented; fixture adapter names notification list API, read-state mutation, and destination payload schema as unknown. | Live notification list API, read/unread mutation, destination payload schema, stale/missing/denied taxonomy, pagination/realtime behavior. | Backend/Notifications: publish notification DTO, destination union, read-state mutation, and pagination/realtime contract. | P0 backend contract blocker |
| V0 | Inbox | `/inbox?conversation=...` covers empty/selected/inaccessible/not found/stale/provider-blocked fixture states. Email deliverability setup is backend-only/no-UI. | Live conversation list/detail transport, send mutation, delivery failure taxonomy, provider-blocked readiness API, message body DTO, realtime/polling policy. | Backend/Messaging: define inbox transport and send/retry contract without exposing DNS/provider internals. | P0 backend contract blocker |
| V0 | Dashboard | API adapter consumes GraphQL `dashboard` counts, `monolith.users`, `monolith.auth`, REST notifications, and GraphQL calendar entries. | Final aggregate semantics, notification activity schema, inbox summary API, calendar readiness/API parity, calendar range/query contract, and replacement card counts/copy. | Backend/Dashboard/Calendar: confirm aggregate ownership, calendar query shape, notification summary, and inbox summary. | P1 schema/payload blocker |
| V1 | Jobs list | Route-state contract is known for scope/search/label/asAdmin/pagination; visual states cover loading/ready/empty/filtered/degraded/stale/unavailable/denied. | Production endpoint, table columns, filter semantics, source health payload, empty reason, pagination envelope, row action fields. | Backend/Jobs: publish Jobs list query params, result row DTO, action availability object, pagination and source-health envelope. | P0 backend contract blocker |
| V1 | Job authoring | Draft normalization/serializer exists; visual states cover create/edit/copy, validating, saving/saved, save failed, publish blocked/partial publish, and resetWorkflow. | Create/update/copy endpoints, final job schema, server validation envelope, resetWorkflow impact payload, draft-vs-published enum, provider-specific publish results. | Backend/Jobs plus Provider: define draft persistence, resetWorkflow summary, and publishing readiness/result contract. | P0 backend contract blocker |
| V1 | Job detail hub | Route sections, parent return, partial degradation, stale-after-task, transition/assignment states are known. | Job aggregate endpoint, section payloads, status transition response, assignment/share payloads, activity schema, partial failure taxonomy. | Backend/Jobs: publish Job detail aggregate and section-level degradation envelope. | P1 schema/payload blocker |
| V1 | Job task overlays | Bid/CV/reject/schedule/offer route shells and lifecycles are known; scheduling consumes calendar readiness; offer consumes contract-signing readiness. | Bid/CV submit endpoints, CV document schema, reject reason catalog, schedule slot/conflict/submit payload, offer payload, signing status, parent refresh trigger. | Backend/Jobs/Candidates/Calendar/Contracts: define mutation schemas and refresh contracts per task kind. | P0 backend contract blocker |
| V1 | Requisition authoring/workflow | Jobs-side build requisition and workflow/stage route-state contracts are known; HRIS readiness/drift states are modeled. | Requisition payload, workflow/stage DTO, HRIS mapping/readiness fields, workflow drift payload, save/submit mutation, stale workflow refresh. | Backend/Requisitions/HRIS: publish requisition workflow aggregate and mutation contracts. | P1 schema/payload blocker |
| V2 | Candidate database | REST `/v2/cv` adapter is confirmed for current local API with include/page/per_page/scope/search/status params and mapped ids, uuid, job/status, names, email, location, source, dates, score, scheduling/offering flags. | Final row schema, column ownership, facets, saved filters/lists, advanced query DSL, bulk mutation schemas, ATS duplicate/import/sync payloads, source freshness metadata. | Backend/Candidates/ATS: decide whether `/v2/cv` remains the production source and publish row/facet/bulk/search contracts. | P0 backend contract blocker |
| V2 | Candidate detail hub | GraphQL adapter is confirmed for candidate profile, job, hiringFlowStep, file, tags, forms, documents, interviewForms, and comments. API-ready database-origin and direct-entry states are captured. | Final candidate aggregate, sequence navigation authority, partial failure taxonomy, contracts/surveys/custom-fields schemas, collaboration/message payloads, stale/not-found/denied/unavailable/mobile contracts. | Backend/Candidates: publish Candidate detail aggregate, sequence API, panel schemas, and error/degradation taxonomy. | P0 backend contract blocker |
| V2 | Candidate action launchers | Schedule/offer/reject task shells and move/hire/unhire/review-request fixture hooks exist; provider setup remains separate; terminal/read-only states are modeled. | Schedule conflict/submit payload, offer/contract payload, reject reason catalog, move-job search/select/mutation, hire/unhire payloads, review request payload, validation/cancel/refresh semantics. | Backend/Candidates/Calendar/Contracts/Review: publish mutation contracts per candidate action. | P0 backend contract blocker |
| V2 | Candidate documents/CV/forms/contracts/surveys/custom fields/collaboration | Panel boundaries and safe telemetry are known; CV upload states distinguish handshake, transfer, metadata persistence, retry, and success. | Upload handshake response, signed URL policy, metadata persistence payload, document/form/contract/survey/custom-field schemas, message/comment/tag mutations, terminal/read-only fields. | Backend/Documents/Forms/Contracts/Surveys/Messaging: publish schemas and upload phases. Signed URLs must stay out of telemetry and docs artifacts. | P0 backend contract blocker |
| V2 | Candidate visual parity states | Behavior evidence exists, plus real local API recapture for database-to-detail handoff. V2 remains parity-blocked and not Figma-ready for replacement. | Denied, unavailable, stale, provider-blocked, validation-error, submit-failure, terminal/read-only, and mobile coverage are incomplete; legacy modal field details are unresolved. | Product/Design plus Backend for matched data: provide same-run matched data and decide explicit parity exceptions if any. | P3 evidence/parity blocker |
| V3 | External chat `/chat/:code` | Public/token screen-flow states are captured for ready, empty, sent, send failure, draft-preserved, token failures, mobile and narrow. | Participant identity fields, thread DTO, live transport, send mutation, delivery error taxonomy, empty-thread reason, draft preservation policy. | Backend/Messaging: publish external chat token payload, live/send transport, and terminal token behavior. | P0 backend contract blocker |
| V3 | Shared job and public application | Route-state and upload failure phases are captured; public/token lifecycle is known at state level. | Public job fields, applicant schema, upload provider fields, upload progress, metadata persistence schema, final submission payload, token/source behavior. | Backend/Public Applications: publish public job/application schema, upload handshake/progress contract, and submission envelope. | P0 backend contract blocker |
| V3 | Public survey and external review/feedback | Ready/validation/submit failure/completed/schema-missing/scoring-pending/terminal states are captured. | Survey question schema, review/feedback answer schema, scoring outcome taxonomy, terminal read-only fields, reviewer/context payloads. | Backend/Surveys/Reviews: publish schema versioning, answer payloads, scoring enum, and terminal read-only DTO. | P1 schema/payload blocker |
| V3 | Requisition approval/forms/download | Approval/reject validation, workflow drift, terminal, used-on-submit, forms view/download/failure/unavailable states are captured. | Approval summary fields, approve/reject mutation payload, workflow drift taxonomy, forms document schema, download/storage provider details, already-downloaded behavior. | Backend/Requisitions/Documents: publish tokenized approval/forms DTOs and download contract. | P1 schema/payload blocker |
| V3 | Integration token entry | CV/forms/job callback state bases are captured; public/token callback is separate from authenticated provider setup. | Integration CV action payloads, conflict taxonomy, requested forms/documents schema, binary transfer/persistence envelopes, integration job payload, used/inaccessible/family-mismatch behavior, provider remediation details. | Backend/Integrations: publish token callback payloads and token lifecycle enum per route family. | P0 backend contract blocker |
| V4 | Settings | Dedicated route/state contracts and compatibility resolver are known. API endpoints are frontend foundation only. Forms/docs and custom-field config use fixture schema ids/source. | Settings payloads, template schemas, reject reason catalog, custom-field schema, forms/docs backend source/version, API endpoint private-token/webhook persistence, public reflection payloads, job-listing publish payloads. | Backend/Settings: publish per-subsection read/update schemas, validation envelope, and downstream refresh/public-reflection behavior. | P0 backend contract blocker |
| V4 | Integrations admin/provider setup | `/integrations*` is separate from `/integration/*`; provider detail covers configuration, auth, diagnostics, readiness handoff for calendar/job-board/HRIS/ATS/assessment; custom remains unavailable/unimplemented. | Provider credential storage payloads, auth lifecycle payloads, diagnostics logs/summaries, readiness signal schema, provider-specific fields, custom provider contract. | Backend/Integrations/Provider teams: publish normalized provider setup/readiness contract and per-family config schemas. | P0 backend contract blocker |
| V4 | Reports | Report index/family/result states, export/schedule commands, stale/partial blocking, and unsupported scheduling are modeled; fixture result marks backend result schema, row dimensions, and metric definitions unknown. | Report metrics/dimensions, result row schema, export payload, schedule payload, delivery/download contract, partial/stale taxonomy. | Backend/Reports: publish report family schemas, export/schedule mutations, and delivery lifecycle. | P0 backend contract blocker |
| V4 | Billing/payment/SMS/card | Billing overview, upgrade, SMS, card, provider challenge, pending/success/failure/retry, card-blocked, stale/degraded/unavailable states are modeled. | Payment tokenization fields, challenge UI contract, card persistence payload, subscription upgrade mutation, SMS mutation, commercial state source, payment failure taxonomy. | Backend/Billing/Payment: publish card/payment and subscription mutation contracts plus provider challenge placeholder rules. | P0 backend contract blocker |
| V4 | Team/invites/recruiter visibility | Team index, recruiters filter/hidden/stale/unavailable/refresh-required, invite unavailable, and action readiness are fixture/state-modeled. | Team member schema, recruiter visibility policy, invite payload, role write policy, invite token behavior, refresh-required semantics. | Backend/Team: publish org team/invite/recruiter visibility schemas and mutation envelopes. | P1 schema/payload blocker |
| V4 | Org favorites/requests/marketplace | Org favorites and requests are separate from platform queue. Marketplace list types and unknown-type unavailable state are known. | Favorite ownership fields, org favorite request mutations, terminal payloads, marketplace item/action schemas, RA assignment/bid/CV item schemas. | Backend/Favorites/Marketplace: publish org favorites/request DTOs and marketplace list/action contracts. | P1 schema/payload blocker |
| V5 | Platform users | Platform `/users*` routes and lifecycle fixture hooks are captured for list/create/detail/edit states. | User list/detail/create/edit schemas, role assignment payload, tenant assignment payload, validation errors, search/filter/pagination semantics, mutation response and conflict/stale behavior. | Backend/Platform Users: publish platform user DTOs, filters, role/tenant assignment schemas, and mutation envelope. | P0 backend contract blocker |
| V5 | Platform favorite requests | Queue/detail pending/resolved/rejected/stale/inaccessible/empty/error/action-failure states are captured and separate from org `/favorites/request*`. | Queue item schema, detail schema, approve/reject/reopen payloads, action failure taxonomy, terminal fields, audit trail fields. | Backend/Platform Favorites: publish favorite-request queue/detail/action contracts and state enum. | P0 backend contract blocker |
| V5 | Platform master data | Company/agency/subscription list/detail/edit shells and non-ready states are captured. Company subscription separates route capability from mutation capability. | Company/agency/subscription schemas, edit mutation payloads, company subscription mutation payload, list filters, stale/not-found/error taxonomy, subscription plan/source fields. | Backend/Platform Master Data/Billing: publish entity schemas and company subscription admin mutation contract. | P0 backend contract blocker |
| V5 | Taxonomy | Sector/subsector list/detail and mutation state hooks are captured with parent targets. | Sector/subsector schemas, create/update/delete/reorder mutation payloads if applicable, parent-child constraints, duplicate/conflict behavior. | Backend/Taxonomy: publish taxonomy DTOs, mutation set, and error taxonomy. | P1 schema/payload blocker |
| V5 | Platform dashboard | Platform dashboard landing/navigation are evidenced; dashboard copy polish is visual debt. | Platform dashboard aggregate widgets, activity schema, tenant/platform metrics, drilldown targets, copy/content ownership. | Backend/Platform: decide whether platform dashboard remains navigation-only or receives production aggregates. | P1 schema/payload blocker |

## Recommended Backend Decision Order

1. Close shared auth/token/session contracts first, because every authenticated and public/token phase depends on predictable failure and continuation behavior.
2. Close notification and inbox contracts next, because V0 resolver behavior is a cross-release entry path and later releases add destination kinds.
3. Close Jobs and Candidate aggregates/mutations together, because V1 task overlays and V2 candidate actions share schedule, reject, offer, document, and parent-refresh semantics.
4. Close provider readiness/setup and upload/download provider contracts before field-specific Figma polish for scheduling, publishing, public application, integration forms, contracts, and billing.
5. Close V4/V5 admin schemas after shared provider/payment/report primitives are settled, so platform/admin tables do not fossilize fixture fields.

## Auth-Specific Source Findings

Local source review for `auth-replacement-readiness-package` found these concrete auth behaviors:

- Login error strings are currently emitted by `ms-auth` as message text and interpreted by the legacy frontend through substring checks. This confirms parity targets for activation, approval, Cezanne mandatory, setup required, invalid credentials, and 2FA states, but keeps structured error-code migration as a P0 blocker.
- Forgot-password uses `mail_not_found`, `mail_sent`, and `mail_error`; the legacy behavior still discloses account-not-found through an error toast.
- Reset password uses `token_not_found`, `token_invalid`, `token_accepted`, and `token_expired`; legacy outcomes redirect home after displaying toast/info copy.
- Confirm registration distinguishes `token_valid` with a returned user/session token from `token_valid` pending approval, then falls back to invalid-token copy.
- Registration invitation uses `token_valid` and `token_invalid` and may prefill invitation fields before returning home with success modal after submission.
- Cezanne and SAML callback transport is confirmed in `ms-auth`, including SAML short-lived code exchange and 422 token-not-found behavior.
- No dedicated legacy `/session-lost` route/copy was located in `/Users/kauesantos/Documents/recruit/frontend` or `/Users/kauesantos/Documents/recruit/ms-auth`; this remains a Product/Auth decision before replacement approval.

## Figma And Replacement Status

Figma drafting status remains separate from backend readiness:
- V1, V3, V4, and V5 may proceed for current screen-flow/base-frame drafting only where their visual evidence docs say so.
- V0 sub-blocks are partial and must carry backend blockers as annotations.
- V2 Candidates remains parity-blocked.
- No V0-V5 row is replacement-approved by this backlog.
