import { AccountSettingsPage } from '../account/account-settings-page';

export function AgencySettingsPage() {
  return <AccountSettingsPage routeKind="agency-settings" titleKey="accountSettings.agency.title" descriptionKey="accountSettings.agency.detail" />;
}
